<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>Results</h1>
    <!-- Recover the recipes recovered from index.js, and render them in this page -->
    <% recipes.forEach(recipe => { %>
        <h2><%= recipe.title %></h2>
        <p><%= recipe.ingredients %></p>
        <p><%= recipe.href %></p>
        <img src="<%= recipe.thumbnail %>">
    <% }) %>
</body>
</html>